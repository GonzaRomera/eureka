let contador, pass, passIngresada: string;

const pass: string = "eureka";

for (let contador: string = 1; contador <= 5; contador++) {
  console.log("ingrese la contraseña nuevamente");
  passIngresada = prompt("ingrese una pass");
  pass == passIngresada;
  contador = contador + 1;
}
console.log("contraseña valida");
